{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/main.cjs",
  "scripts": {
    "dev": "electron src/main.cjs",
    "build": "tsup src/main.ts --format cjs --out-dir dist && tsup src/preload.ts --format cjs --out-dir dist",
    "start": "electron dist/main.cjs"
  },
  "dependencies": {
    "electron": "^32.2.5",
    "@bildbot/shared": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsup": "^8.3.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
